const initialState = {
  currentUser: {

  },
  loggedIn: true
}

const sessionsReducer = (state = initialState, { type, payload }) => {
  switch (type) {

  default:
    return state
  }
}

export default sessionsReducer;
